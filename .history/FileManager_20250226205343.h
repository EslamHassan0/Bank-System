#include <iostream>
#include <vector>
#include <string>
using namespace std;

class FileManager : IDataSource {
public:
    static vector<string> split(string line) {
       // return nullptr;
    }                        
};
