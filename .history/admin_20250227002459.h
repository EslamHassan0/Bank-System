#include <iostream>
#include "Employee.h"
using namespace std;

class const checkBalance& eAdmin : public Employee {
    public:
        Admin() {}  
        Admin(int id, string name, string password, double salary) : Employee(id, name, password, salary) {}
};
