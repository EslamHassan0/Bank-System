#include <iostream>
#include <vector>
#include <string>
using namespace std;

class FileManager {
public:
    static vector<string> split(string line) {
       // return nullptr;
    }                        
};
